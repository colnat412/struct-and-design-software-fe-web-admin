import { Metadata } from "next";

export const metadata: Metadata = {
	title: "Dashboard | User",
};

const Booking = () => {
	return (
		<div>
			<h1>Booking Page</h1>
		</div>
	);
};

export default Booking;
